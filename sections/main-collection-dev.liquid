<section id="wrapper-{{ section.id }}">
    <div class="product-container-{{ section.id }}">
        {% for collection in collections %}
            {% if collection.all_products_count > 0 %}
                {% for product in collection.products %}
                    {{ product.title }}
                {% endfor %}
            {% endif %}
        {% endfor %}
    </div>
</section>