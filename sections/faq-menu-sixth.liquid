<div class="page-width faq-page-width">
  <section id="faq-six" class="faq-menu-contents">
     {% if section.settings.faq_menu_title %}
    	<a class="faq-menu" href="#faq-six">{{ section.settings.faq_menu_title }}</a>
    {% endif %}
    {% for block in section.blocks %}
    {% case block.type %}
    {% when 'question' %}
    <div class="faq-card">
      <a href="#">{{ block.settings.title }}<i class="custom-tab-icon tab-open"></i></a>
      <div class="content">
        {{ block.settings.text }}
      </div>
    </div>
    {% endcase %}
    {% endfor %}
  </section>
</div>
{% schema %}
  {
    "name": "Payment & Gift Cards",
	"class": "faq-six",
    "settings": [
            {
              "type": "text",
              "id": "faq_menu_title",
              "label": "Sixth FAQ Menu",
              "default": "Voting FAQs"
            }
		],
	"blocks": [
		{
		"type": "question",
        "name": "Question",
        "settings": [
          {
            "type": "text",
            "id": "title",
            "label": "Question",
            "default": "Frequently asked question"
          },
          {
            "type": "richtext",
            "id": "text",
            "label": "Text",
            "default": "<p>Use this text to answer questions in as much detail as possible for your customers.</p>"
          }
        ]
		}
		]
  }
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}
